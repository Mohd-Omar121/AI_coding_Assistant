npm run dev
cd coding-assistant
ps aux | grep vite
npm run dev
pwd && ls -la
# OpenAI CODEX Configuration
VITE_OPENAI_API_KEY=your_openai_api_key_here
VITE_OPENAI_API_URL=https://api.openai.com/v1
VITE_CODEX_MODEL=code-davinci-002
cd coding-assistant
pwd
echo "# ChatGPT 4.1 Configuration" > .env && echo "VITE_OPENAI_API_KEY=your_openai_api_key_here" >> .env && echo "VITE_OPENAI_API_URL=https://api.openai.com/v1" >> .env && echo "VITE_CHATGPT_MODEL=gpt-4o-mini" >> .env
ls -la public/
mkdir -p public && echo "Creating public directory for assets"
ps aux | grep "npm run dev" | grep -v grep
npm run dev

